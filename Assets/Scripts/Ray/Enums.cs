using System.Collections;
using System.Collections.Generic;

namespace Tsutaeru
{
    public enum GameState { InGame, OutGame }
    public enum CharacterName { Boy, Girl}
    public enum Emotion { Normal, Happy, Sad, Nervous, NervousMuch, HappyNervous }
}